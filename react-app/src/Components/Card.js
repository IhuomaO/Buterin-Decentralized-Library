import React from "react";

const Card = ({ id, bgImage, body, value, name, cardIds, setCardIds, ...props }) => {
  return (
    <div className="bg-white w-60" {...props}>
      testing
    </div>
  );
};

export default Card;
